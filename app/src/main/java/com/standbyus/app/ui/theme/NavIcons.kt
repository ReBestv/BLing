package com.standbyus.app.ui.theme

import androidx.annotation.DrawableRes
import com.standbyus.app.R

/** Drawable resource IDs for bottom nav icons — loaded via painterResource() in BottomNavBar. */
object NavIcons {
    @DrawableRes val Home: Int = R.drawable.home
    @DrawableRes val Checkin: Int = R.drawable.checkin
    @DrawableRes val Album: Int = R.drawable.album
    @DrawableRes val Timeline: Int = R.drawable.history
    @DrawableRes val Todo: Int = R.drawable.todo
}
